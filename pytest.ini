[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py *_tests.py
pythonpath = .
addopts = -ra --reuse-db
norecursedirs = ai-technews-admin-intake-main .git .venv venv env
markers =
    integration: Integration tests hitting live network resources or databases.

